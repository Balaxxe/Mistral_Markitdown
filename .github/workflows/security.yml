name: Security

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '0 8 * * 1'  # Weekly Monday 8am UTC

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Runner/image setuptools can be older than PYSEC-2026-3447 fix (83.0.0).
          python -m pip install --upgrade "setuptools>=83.0.0"
          pip install -r requirements.txt
          pip install pip-audit

      - name: Run pip-audit
        run: pip-audit --desc

      - name: Install Bandit
        run: pip install bandit

      - name: Run Bandit security linter
        run: bandit -r . --exclude ./env,./venv,./.venv,./tests,./build,./dist -ll --format json --output bandit-report.json

      - name: Display Bandit results
        if: always()
        run: |
          if [ -f bandit-report.json ]; then
            python -m json.tool bandit-report.json
          fi
